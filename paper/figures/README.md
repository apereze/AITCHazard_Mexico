# Figure Workspace

This directory is for lightweight, publication-oriented figure assets.

Recommended practice:

- Track final or near-final figures only when they are lightweight.
- Keep large intermediate images and rendering outputs outside Git.
- Document the script, notebook, or workflow used to regenerate each final figure.
- Use clear filenames such as `figure_01_workflow.png` when figures are ready for manuscript use.
