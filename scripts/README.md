# Scripts

Command-line scripts and workflow entry points should live here.

Current structure:

- `block1/`: Block 1 script workspace.
- `block1/legacy/`: initial AIFS runner prototypes preserved for refactoring.

Production scripts should be small, documented, and connected to configuration files rather than local machine paths.
