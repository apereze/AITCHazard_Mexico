# Data

This directory is a local staging area for project data.

Large raw, intermediate, and processed datasets are intentionally ignored by Git. Document dataset provenance and regeneration steps in `docs/data-governance.md` or companion metadata files.
