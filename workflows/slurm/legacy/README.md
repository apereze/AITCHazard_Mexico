# Legacy SLURM Scripts

These SLURM scripts are preserved as initial HPC prototypes.

They are not yet portable because they contain hard-coded paths, cluster-specific module names, AIFS-ens container names, and personal log directories. Keep them as references while building cleaned workflow templates.
