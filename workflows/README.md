# Workflows

Future orchestration files should live here, including HPC job scripts, reproducibility workflows, or pipeline definitions.

Workflow files should document required environment variables, input locations, and expected outputs.
