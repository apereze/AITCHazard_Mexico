# Workflows

Orchestration files should live here, including HPC job scripts, reproducibility workflows, or pipeline definitions.

Current structure:

- `slurm/`: SLURM workflow workspace.
- `slurm/legacy/`: initial HPC prototypes preserved for refactoring.

Workflow files should document required environment variables, input locations, and expected outputs.
