# Configuration

This directory contains project configuration files.

Current intent:

- `aitchazard_mexico/`: AITCHazard-specific configuration sketches that adapt SwAIther-Precip concepts to Mexico.

These files are not yet production training configs. They define the expected interface and naming conventions before the full SwAIther adaptation is implemented.
